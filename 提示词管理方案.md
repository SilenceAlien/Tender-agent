# 标书项目 — 跨工具提示词管理方案

## 一、问题定义

标书撰写是一个长周期、多窗口、多工具协作的过程。核心痛点：

| 痛点 | 表现 |
|------|------|
| **上下文断裂** | 每开一个新窗口，之前的对话上下文全部丢失 |
| **提示词分散** | 不同章节的优质 prompt 散落在各处，无法复用 |
| **策略不统一** | 换个工具/窗口后，标书撰写风格和策略偏离 |
| **决策无法追溯** | 上周做的关键决策，这周找不到了 |

## 二、核心设计理念

> **以文件系统为共享总线，以 `.workbuddy/` 为配置中心。**

项目根目录下的 `.workbuddy/` 是所有 WorkBuddy 实例读取配置的唯一入口。不管你在 VS Code、终端还是另一个电脑上打开同一个项目，读到的都是同一份配置。这就是天然的跨工具、跨窗口共享机制。

## 三、四层架构

```
标书agent02/
├── .workbuddy/                    ← 配置中心（GIT 可追踪）
│   ├── memory/                    ← Layer 1: 上下文共享
│   │   ├── MEMORY.md              # 精简索引（≤500t），存核心上下文+资料路径
│   │   ├── reference/             # Tier 2: 详细资料（按需加载）
│   │   │   ├── 术语表.md
│   │   │   ├── 评分标准.md
│   │   │   ├── decisions.md       # 历史决策归档
│   │   │   ├── 客户分析.md
│   │   │   └── 竞品分析.md
│   │   └── YYYY-MM-DD.md          # 每日工作日志（决策追溯）
│   ├── prompts/                    ← Tier 3: 提示词模板库（独立资产，非记忆）
│   │   ├── 技术方案/
│   │   │   ├── 系统架构.md
│   │   │   ├── 技术路线.md
│   │   │   └── 项目实施.md
│   │   ├── 商务方案/
│   │   │   ├── 公司介绍.md
│   │   │   ├── 业绩案例.md
│   │   │   └── 服务承诺.md
│   │   ├── 报价方案/
│   │   │   └── 报价策略.md
│   │   └── 通用/
│   │       ├── 格式规范.md
│   │       ├── 评分响应模板.md
│   │       └── 上下文预算管理.md
│   │   └── 标书类型/                 # 按采购类型的专用模板（与章节模板叠加使用）
│   │       ├── 服务采购.md
│   │       ├── 货物采购.md
│   │       └── 软件开发.md
│   ├── skills/                    ← Layer 2: 可复用技能
│   │   ├── 招标文件分析/           # SKILL.md: 解析招标书、提取评分标准
│   │   ├── 标书章节生成/           # SKILL.md: 按模板生成各章节
│   │   ├── 评分点映射/             # SKILL.md: 将评分标准映射到响应段落
│   │   ├── 质量合规审查/           # SKILL.md: 一致性检查、格式校验
│   │   └── 上下文注入/             # SKILL.md: 按预算加载Tier1+2+3，拼接prompt
│   └── agents/                    ← Layer 3: 专家代理
│       ├── 标书撰写专家.md          # 全局统筹、风格统一
│       ├── 技术方案专家.md          # 技术架构、实施方案
│       ├── 商务方案专家.md          # 商务条款、业绩包装
│       └── 格式审查专家.md          # 格式合规、排版检查
├── 招标文件/                       ← 原始招标材料
├── 参考资料/                       ← 公司资质、案例库
└── 输出/                           ← 生成的标书文件
```

### Layer 4: 自动化

通过 WorkBuddy 内置的 Automation 系统实现：

| 自动化任务 | 触发方式 | 作用 |
|-----------|---------|------|
| 每日上下文同步 | 每天 9:00 | 将昨日所有修改汇总到 MEMORY.md |
| 标书章节生成 | 手动触发 | 一键启动某章节的生成流程 |
| 评分覆盖率检查 | 提交前 | 逐条核验评分点是否被响应 |

## 四、跨工具共享原理

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  WorkBuddy  │    │  WorkBuddy  │    │  VS Code    │
│  窗口 A     │    │  窗口 B     │    │  + 终端     │
└──────┬──────┘    └──────┬──────┘    └──────┬──────┘
       │                  │                  │
       └──────────────────┼──────────────────┘
                          │
                    读取/写入同一份
                   ┌─────▼─────┐
                   │.workbuddy/ │  ← 项目根目录下的配置中心
                   │  memory/   │  ← 共享上下文（MEMORY.md）
                   │  skills/   │  ← 可复用技能（Skill tool 加载）
                   │  agents/   │  ← 专家代理（Expert 系统加载）
                   │  prompts/  │  ← 提示词模板（任意工具可读）
                   └───────────┘
```

**关键机制**：

1. **文件系统即共享总线** — `.workbuddy/` 是普通文件目录，任何工具都能读写
2. **MEMORY.md 即共享大脑** — 所有窗口的 WorkBuddy 自动读写同一份记忆
3. **Skills 即共享能力** — 通过 Skill tool 加载，跨窗口复用同一套工作流
4. **Cloud Memory 即跨机器同步** — 服务端自动注入的用户画像，跨设备生效
5. **Git 友好** — 整个 `.workbuddy/` 可以纳入版本管理，团队协作

## 五、所需工具与 Skill 清单

### 已内置（无需安装）

| 工具/Skill | 作用 |
|-----------|------|
| **Memory System** | 三层记忆（Cloud / User / Workspace），实现跨会话上下文持久化 |
| **Skill Management** | 创建/修改项目级 Skill，封装可复用的提示词工作流 |
| **Expert/Agent System** | 创建领域专家 Agent，嵌入专用提示词和知识 |
| **Automation System** | 定时任务与触发式自动化，携带预设上下文执行 |
| **`skill-creator`** | Skill 创建向导，快速生成规范的 SKILL.md |
| **`expert-manager`** | Expert 包管理，创建/编辑/审查专家定义 |
| **`prompt-engineering-expert`** | 提示词工程指导，优化 Prompt 结构和效果 |

### 需要创建的项目级 Skill

| Skill 名称 | 功能 | 触发场景 |
|-----------|------|---------|
| **`招标文件分析`** | 读取招标 PDF/docx，提取评分标准、关键要求、响应清单 | "分析这份招标文件" |
| **`标书章节生成`** | 基于模板 + 上下文生成指定章节，自动注入 MEMORY.md 的策略 | "生成技术方案章节" |
| **`评分点映射`** | 将评分标准逐条映射到标书段落，输出覆盖率报告 | "检查评分点覆盖" |
| **`质量合规审查`** | 一致性检查（术语、格式、编号）、敏感词扫描、响应完整性 | "审查标书质量" |
| **`上下文注入`** | 读取 MEMORY.md + prompts/ 模板，自动拼装完整提示词 | 被其他 Skill 内部调用 |

### 需要创建的 Expert Agent

| Expert 名称 | 职责 | 内嵌提示词要点 |
|-------------|------|--------------|
| **`标书撰写专家`** | 全局统筹者，负责风格统一、章节衔接、整体质量把控 | 标书结构规范、政府/企业风格适配 |
| **`技术方案专家`** | 技术架构设计、实施计划、技术亮点包装 | 技术方案框架、创新点提炼、架构图描述 |
| **`商务方案专家`** | 公司介绍、业绩包装、商务条款响应 | 业绩写法、资质展示、服务承诺话术 |
| **`格式审查专家`** | 排版检查、格式一致性、合规校验 | 格式规范清单、常见错误模式 |

## 六、实施步骤

### 第一步：初始化目录结构

创建 `.workbuddy/` 及其子目录，建立 MEMORY.md 骨架。

### 第二步：填充 MEMORY.md（共享上下文）

```markdown
# 标书项目 — 共享上下文

## 当前项目
- 项目名称: [待填写]
- 客户: [待填写]
- 投标类型: [政府采购 / 企业招标 / ...]
- 截止日期: [待填写]

## 标书策略
- 核心差异化优势: [待填写]
- 报价策略: [待填写]
- 风格基调: [专业严谨 / 创新突出 / 稳健可靠]

## 约定规范
- 术语表: ...
- 格式要求: ...
- 禁用词/敏感词: ...

## 已做出的关键决策
- [日期] 决策: ...  理由: ...
```

### 第三步：创建提示词模板库

为每个章节创建标准化的提示词模板，模板中包含：
- 章节目标（该章节要达成的投标效果）
- 结构框架（必含段落和顺序）
- 评分对标（对应哪几条评分标准）
- 上下文占位符（`{客户}`, `{项目名}` 等变量）
- 输出格式要求

### 第四步：封装为 Skills

将上述模板和流程封装为可调用的 Skill，使其可以通过自然语言触发。

### 第五步：创建 Expert Agents

为每个专业领域创建 Expert Agent，内嵌领域知识和工作流程。

### 第六步：配置自动化

设置日常的上下文同步、质量检查等自动化任务。

## 七、日常使用流程

```
1. 打开 WorkBuddy（任意窗口）
   └→ 自动加载 .workbuddy/skills/ 下的所有 Skill
   └→ 自动加载 .workbuddy/agents/ 下的 Expert
   └→ 自动读取 .workbuddy/memory/MEMORY.md 作为上下文

2. 说出需求
   └→ "用标书撰写专家，帮我生成第三章技术方案"
   └→ Expert 自动从 MEMORY.md 获取项目上下文
   └→ Expert 自动从 prompts/ 加载模板
   └→ 生成内容自动符合风格和策略

3. 切换到另一个窗口
   └→ 打开同一项目
   └→ 读到同一份 MEMORY.md → 上下文完全一致
   └→ 加载同一批 Skills → 能力完全一致

4. 换一台电脑
   └→ git clone 项目
   └→ Cloud Memory 自动注入用户画像
   └→ .workbuddy/ 配置完全同步
```

## 八、上下文窗口管理 ⚠️ 关键设计

> 这是方案中最容易被忽视但最重要的部分。如果不加控制，MEMORY.md + 模板 + Skill + 对话历史的 token 总和会轻易超出上下文窗口。

### 三层加载策略

```
Tier 1: 常驻层 (目标 ≤500 tokens，硬上限 800)
  └→ MEMORY.md 精简版（项目名、客户、核心策略、评分要点、当前阶段）

Tier 2: 场景加载层 (按任务类型触发，每次 1-2 个文件，≤2000 tokens)
  └→ reference/ 目录下的详细资料（术语表、评分标准、客户分析等）
  └→ 加载前向用户确认："判断任务为 XX 类型，将加载 A 和 B，需要调整吗？"

Tier 3: 显式加载层 (用户明确要求或 Skill 内部引用时才加载)
  └→ 提示词模板（.workbuddy/prompts/*.md）
  └→ 历史日志、参考文档
```

### 三层各自的职责边界

| 层 | 存储位置 | 加载时机 | 大小约束 | 维护方式 |
|----|---------|---------|---------|---------|
| Tier 1 | `memory/MEMORY.md` | 每次对话自动 | 目标 500t，硬上限 800t | 手动 + Skill 辅助拆分 |
| Tier 2 | `memory/reference/` | 按任务类型按需 | 单文件 ≤ 1000t | 正常积累 |
| Tier 3 | `prompts/` | 显式引用 | 无硬限制 | 模板迭代 |

> **重要区分**：`prompts/` 不是 `memory/` 的子目录。提示词模板是**工具/资产**，不是记忆。它们位于 `.workbuddy/prompts/` 作为独立资产层。

### MEMORY.md 设计约束

| 约束 | 值 |
|------|-----|
| 目标 token 数 | ≤ 500 |
| 硬上限 | ≤ 800 |
| 拆分方式 | **手动 + Skill 辅助**（WorkBuddy 无守护进程，不存在真·自动拆分） |
| 拆分触发 | 上下文本维护 Skill 检测到超限时提示用户 |
| 历史决策保留 | 最近 3-5 条，旧决策归档 |
| 详细资料 | 只存路径索引 + 预估 tokens，不存实际内容 |

**为什么不是真·自动拆分**：WorkBuddy 没有后台守护进程，无法持续监控文件大小。实际做法是通过「每日上下文同步」自动化任务检测并提示，或用户手动触发「上下文本维护」Skill 来执行拆分。

### 全链路 Token 预算模型

上下文消耗 = 注入层 + 对话层，两者都要控制：

```
┌─────────────────────────────────────────────┐
│  注入层（本次任务需要的新上下文）               │
│  Tier 1 MEMORY.md        ~500-800 tokens     │
│  Tier 2 reference 文件    ~500-1000 tokens   │
│  Tier 3 提示词模板         ~500-800 tokens   │
│  Skill 自身定义           ~300-500 tokens    │
│  小计                     ~1800-3100 tokens  │
├─────────────────────────────────────────────┤
│  对话层（已有对话的累积）                      │
│  系统提示词               ~2000+ tokens       │
│  对话历史（N 轮）          ~500-5000+ tokens  │
│  生成输出                  ~1000-3000 tokens  │
│  小计                     ~3500-10000 tokens  │
├─────────────────────────────────────────────┤
│  总消耗（典型场景）         ~5300-13100 tokens │
└─────────────────────────────────────────────┘
```

**对话层才是上下文溢出的主要风险源**，而非注入层。关键应对：

- **每 15-20 轮对话后执行摘要压缩**：用 Skill 将本轮关键决策写入 MEMORY.md，然后开新对话
- **长章节生成建议开新窗口**：避免同一窗口内累积过多历史
- **单个对话窗口只处理 1-2 个章节**

### 降级策略（明确触发方式）

| 触发条件 | 动作 | 触发方式 |
|---------|------|---------|
| MEMORY.md > 800t | 拆分详细内容到 reference/ | 上下文维护 Skill（手动或自动化触发） |
| 决策条目 > 5 条 | 旧条目归档到 reference/decisions.md | 同上 |
| 每日日志 > 30 天 | 合并归档，删除旧文件 | 自动化任务（每月执行） |
| 注入层合计 > 2500t | 提示缩小任务范围 | 上下文注入 Skill 实时检测 |
| 对话 > 15 轮 | 建议执行摘要压缩 | Skill 提示用户 |

### 上下文注入 Skill 的工作流（修正版）

```
用户请求
  │
  ▼
上下文注入 Skill
  ├─→ 读取 MEMORY.md（Tier 1）
  ├─→ 判断任务类型 → 匹配 Tier 2 文件
  ├─→ 【协商】向用户确认加载选择："我判断为 XX 任务，将加载 A 和 B，需要调整吗？"
  ├─→ 按需加载提示词模板（Tier 3，1个）
  ├─→ 拼接 prompt，估算总 tokens
  ├─→ 如注入层 > 2500t → 提示用户缩小范围
  ├─→ 如对话已 > 15 轮 → 建议压缩后再继续
  └─→ 交给 Expert Agent 执行
```

详细策略见：`.workbuddy/prompts/通用/上下文预算管理.md`

## 九、进阶优化

- **Prompt 版本管理**：`prompts/` 目录纳入 Git，可以 diff 对比提示词变更
- **A/B 测试**：同一章节维护 v1/v2 两份模板，对比生成效果
- **团队共享**：`.workbuddy/` 提交到仓库，团队成员克隆后开箱即用
- **效果日志**：在每日日志中记录每个 prompt 的生成效果，持续迭代
