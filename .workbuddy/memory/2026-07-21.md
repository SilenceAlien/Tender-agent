# 2026-07-21 工作日志

## 标书agent 全流程冒烟测试（test001 劳务外包标书）
- 用户称已在别处修复上一轮发现的 F3(资质FAIL静默dead-end)、F4(30k硬截断)。要求跑一轮全流程测试，模拟提交劳务外包标书：投标人=广州华南人力、标号=test001。
- 新增 harness `bid-agent/scripts/smoke_labor_test001.py`（仅运行，未改源码）。输入=项目根 `2026年广州铁道车辆有限公司劳务管理服务采购（技术标）v2.pdf`（劳务外包类真实PDF）。无 API Key → LLM 节点 mock。
- **结论：全流程跑通，F3/F4 修复均生效**：
  - 14 节点全 `completed`（FeedbackProcessor=pending 正常）；生成 9 章节；导出 `data/exports/广州华南人力_标书_v1.docx`。
  - F3 验证：EligibilityChecker 不再 return __end__，流水线继续到导出。
  - F4 验证：DocumentParser 解析 12 chunks + 14 tables，无截断崩溃。
  - 投标人 广州华南人力 贯通：进入 project_contract.bidder_name 并落到文件名（广州华南人力_标书_v1.docx）。
- **观察项（非回归）**：
  - 标号 test001 未进产物：mock 无 LLM 抽不出 bid_number；state 预置的 requirements.bid_number 被 ReqExtractor 表驱动重建覆盖；即便 user_confirmed_fields 注入 bid_number 也只映射到 project_contract.project_code，而文件名 safe_id 只用 requirements.bid_number 或 project_contract.bidder_name（不含 project_code）→ 不落文件名。属架构/环境限制，需真实 Key 或 GUI 表单字段写入 requirements.bid_number。
  - 正文为 mock 占位（75–87字通用模板），内容质量/一致性需配 Key 重跑。
- 交付：`specs/code-audit/smoke_test001_report.md`。

## 标书agent 逐节点功能测试（SHEIN docx，09:38 续）
- 用户要求用真实 docx 招标文件 `附件一：招标文件--2026年度SHEIN质检业务外包项目.docx` 跑全流程并逐节点查功能。新增 harness `bid-agent/scripts/smoke_labor_test001_docx.py`（仅运行）。
- **结论：14 节点全 completed，0 失败；F3/F4 修复在 docx 下复验通过；无节点功能缺陷。**
- 逐节点亮点：DocumentParser 解析 14 chunks+7 tables（docx 正常）；ReqExtractor 子类型识别 `BPO` 正确（质检外包→BPO）；ContractExtractor 投标人 广州华南人力 贯通、industry=人力资源（劳务外包类映射正确）；InfoVerificationGate confirmed+10字段 summary；SectionGenerator 9 章节生成；QualityChecker/ComplianceChecker/ScoreSimulator 结构完整且 verdict=PASS（mock 无 issue/ scoring 项，诚实返回"无法评估"）。
- 观察项（均非 bug，mock 限制）：① TemplateMatcher matched_templates=[]（requirements 空→query 空→template_matcher.py:171 早返；SectionGenerator 有兜底仍产 9 章）；② ReqExtractor scoring/qual=0（mock 无 LLM，7 表未归类 scoring/qual 类型；docx 表格已 `_classify_table` 正确分类，非解析缺陷）；③ QualityChecker total=1/passed=0/PASS 为 max(1,total) 强制最小计数（quality_checker.py:653），verdict 正确；④ 正文 mock 占位，test001 仍不进产物（同架构限制）。
- 交付：`specs/code-audit/smoke_test001_docx_report.md`。
