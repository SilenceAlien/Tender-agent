# 2026-07-11 工作日志

## 标书智能体全流程功能测试（端测测）

### 完成内容
- 创建隔离 venv（bid-agent/.venv，Python 3.13.12），安装全部依赖
- 运行 pytest 套件：**507 通过 / 0 失败**（65s）
- Streamlit UI 冒烟测试：HTTP 200，四面板正常加载
- 动态功能测试（mock LLM + 真实 21MB PDF）：覆盖 13 节点 + E2E，发现 14 个问题
- 静态代码审查：审查全部核心节点 + GUI 面板代码
- 汇总到 bugs.md：旧 bug 状态核查（11/15 已修复）+ 14 个新问题（N01-N14）

### 关键发现
- **N01 [高]** headless 管道 export_path 丢失：export_path 不在 AgentState，被 LangGraph 丢弃，文件生成了但调用方拿不到路径
- **N02 [高]** 交互模式 DocumentAssembler 永不执行：build_generation_graph 的 HumanReviewGate 无 auto_approve，导出完全靠 export_panel 按钮
- **N03 [高]** config_panel DS 模型名虚构：deepseek-v4-pro/flash 不存在，实际是 deepseek-chat/reasoner
- **N05/N06/N07** CrossReferenceChecker 缺契约偏离/技术参数检查；ComplianceChecker 不检查 format_rules（PRD 验收标准未满足）
- **N08** 标书类型命名三处不一致，知识库缺"软件类"目录
- 测试盲区：pytest 507 全通过但未覆盖上述场景

### 交付物
- bugs.md：追加完整测试报告（旧 bug 核查 + 14 个新问题 + 盲区分析 + 优先级建议）
- 测试脚本：/tmp/bid_smoke/smoke_test.py
- 问题列表：/tmp/bid_smoke/issues.json

### 环境备注
- bid-agent/.venv 已创建（Python 3.13.12 managed），后续测试可直接用 `.venv/bin/python`
- 运行 streamlit：`cd bid-agent && .venv/bin/python -m streamlit run gui/app.py`
- 运行 pytest：`cd bid-agent && .venv/bin/python -m pytest tests/ -v`

## 提取 bugs02.md
- 将 bugs.md 第三节「新发现问题 N01~N14」完整提取到 `/Users/alanchris/Desktop/标书agent02/bugs02.md`
- 仅记录不修复，保留每个问题的文件/严重程度/复现/影响/修复建议 + 优先级表

## 第二轮回归测试（用户修复 N01-N14 后）

### 完成内容
- pytest：**507 通过 / 0 失败**（17.9s）
- Streamlit UI 冒烟：HTTP 200，节点级模型配置 UI 已出现
- 动态功能测试（mock LLM + 真实 PDF）：14 项修复点专项验证
- N01-N14 修复验证：**12/14 已确认修复**，N02 仅文档化，N11 修复不彻底

### 关键发现（新问题写入 bugs03.md）
- **R01 [高]** config_provider 恒为 deepseek（N11 修复不彻底）：config_panel.py:128 `"deepseek" if ds_model else ...`，ds_model 恒非空 → provider 永远 deepseek。只配 OpenAI 的用户，未设 node_override 的节点全部 fallback 到 deepseek（无 key → mock），系统不可用
- **R02 [中]** 交互模式 approved 路径死代码（N02 仅文档化）：review_panel 缺 resume_after_review 调用，graph.py 声明的 approved → DocumentAssembler 路由从未触发，导出仍靠 export_panel 按钮

### 交付物
- bugs03.md：第二轮回归测试报告（修复验证表 + R01-R02 新问题 + 盲区分析）
- 测试脚本：/tmp/bid_smoke/regression_test.py
