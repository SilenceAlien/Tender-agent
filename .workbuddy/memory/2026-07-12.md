# 2026-07-12 工作日志

## 超大招标文件读取策略 — 并行评估

### 任务
- 对 specs/009-large-file-strategy/spec.md (v5修订版) 进行多agent并行评估
- 7个agent分别评估7个模块，只评估不执行

### 评估结果
- 综合评分：6.6/10
- 发现13个Critical级问题（7个P0阻塞性 + 6个P1实施中解决）
- 核心问题：document_id生成bug、sections字段类型冲突、AgentState未更新、bigram英数处理bug、PageBlobReader写入未集成、_calc_ocr_workers竞态、PaddleOCR线程安全
- 原方案22-30天 → 修订为33-42天（含Phase 0修复3-4天 + Phase 11联调3-5天）

### 产出文件
- /specs/009-large-file-strategy/evaluation-report.md — 综合评估报告

### 关键决策
- 建议先修复7个P0问题再进入正式实施
- 验收标准召回率目标统一为90%（原方案95%/90%不一致）
- PaddleOCR环境需单独验证（线程安全、API版本兼容性）

## 超大招标文件读取策略 — 方案优化（v5→v6）

### 任务
- 根据评估报告建议，优化spec.md方案文档（只优化不执行）
- 修复13个Critical问题 + 多项P2优化

### 优化内容
1. **C-01**: document_id 改用 md5(filename + file_size + 前1MB内容hash)，不依赖临时路径
2. **C-02**: sections 字段全局重命名为 chapter_index，避免与 AgentState 顶层冲突
3. **C-03**: _calc_ocr_workers 改为 per-task 信号量限流 + 非阻塞快照
4. **C-04**: AgentState TypedDict 扩展 + factory_state 默认值
5. **C-05**: _tokenize_query 统一切片逻辑，英数整词查询与索引一致
6. **C-06**: _serialize_state 检测 streaming 模式跳过全文深拷贝
7. **C-07**: _stream_parse_pdf 统一使用 PageBlobReader.write_page
8. **C-08**: PaddleOCR 改为线程级实例池替代全局单例
9. **C-09**: _ocr_with_timeout 改用 multiprocessing.Process + terminate()
10. **C-10**: _detect_chapter_boundary 补全实现 + probe_info 参数 + TOC优先
11. **C-11**: _stream_parse_pdf 返回5元组含元信息dict，透传给_build_meta
12. **C-12**: 进度文件 tmp+os.replace 原子写入 + 每50页增量落盘索引
13. **C-13**: IncrementalIndexBuilder 增加反向索引 _page_to_tokens，O(K)替代O(N)

### P2优化
- LRU容量 50→200页
- per_page_limit 按类别差异化（scoring/qual 1200, tech 800, format 600）
- QualityChecker公司名改用倒排索引搜索替代前20页
- 磁盘预检 2GB→3GB
- 验收标准召回率统一为90%，OCR识别率95%→85%
- PaddleOCR版本锁定 paddleocr==2.7.0.3 + paddlepaddle==2.6.1
- 实施路径增加Phase 0(3-4天) + Phase 11联调(3-5天)
- _extract_company_names 扩展正则（合伙企业/事务所/研究院/设计院/大学等）
- 信号量增加超时(300s)避免无限阻塞

### 产出文件
- /specs/009-large-file-strategy/spec.md — v6修订版（2604行→3001行，+397行）
- /specs/009-large-file-strategy/evaluation-report.md — 评估报告（上一轮产出）
