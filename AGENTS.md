# AGENTS.md — 项目开发规范

> 环境无关设计：本规范适用于 WorkBuddy / Claude Code / Cursor 等任意 AI 编码环境。

---

## 一、Skill 自进化机制

每次解决了一个值得记住的问题，必须固化为 skill 文件。

### 触发条件

| 条件 | 触发信号 | 写入目标 |
|------|---------|---------|
| 用户纠正 | "不对"、"应该这样"、"换成 X" | 项目级 → `AGENTS.md`；通用行为 → 用户级配置；可复用方案 → skills 目录；项目技术细节 → `specs/[feature]/research.md` |
| 可复用工作流 | 完成 ≥3 步操作序列 + 不依赖本项目特有框架 + 可抽象为通用模板 | skills 目录（满足 ≥2 项条件时） |
| 隐性规则 | 同一约束在 ≥2 个会话中出现 | 项目级 → `AGENTS.md`；用户级 → 用户配置 |

**禁止**：纠正后不持久化；创建空泛 skill；覆盖已有规则不做 diff。

---

## 二、核心原则（6 条，不可协商）

1. **规范先行** — 任何代码改动前必须先有规范文档。专注"什么"和"为什么"，禁止过早涉及实现细节。
2. **证据驱动** — 测试先于实现，测试通过 + review 通过才算完成。禁止"我说它对了所以它对了"。
3. **增量交付** — 每个用户故事独立可测试。MVP 先行，迭代改进。只实现当前需要的功能（YAGNI），禁止提前设计未来扩展点。
4. **代码质量** — lint 通过，API 有文档，遵循项目风格。
5. **可追溯性** — 需求 → 规范 → 计划 → 任务 → 实现 完整追溯链。
6. **原则高于实践** — 所有决策必须符合以上原则。

---

## 三、核心工作流程

```
头脑风暴（苏格拉底式提问澄清需求）
    ↓
创建功能规范 → specs/[###-feature]/spec.md（含「需求背景」章节）
    ↓
创建技术计划 → plan.md / research.md / data-model.md
    ↓
分解原子任务 → tasks.md（每个 2-5 分钟，含精确文件路径和验证方式）
    ↓
执行实现（见下方「任务执行循环」）
    ↓
收尾：全量测试 → lint → 合并 → 清理 → 文档更新
    ↓
自进化检查：本会话有纠正？→ 持久化 / 有可复用工作流？→ 提取为 skill
```

### 任务执行循环（每个任务）

```
[Fast] 写测试 → harness 确认红（失败）
[Fast] 写实现 → harness 确认绿（通过）
[Pro]  Spec Compliance Review（对比 spec.md 验收场景）
[Pro]  自动 Code Quality Review（逻辑/安全/性能/类型）
   ↓ 有漏洞 → 直接修复
[Fast] 重构 → harness 确认仍通过
```

- 每个任务 = 一个全新的子 Agent（上下文清零，避免腐化）
- 无依赖任务并行分发，有依赖任务串行执行
- 同一操作最多重试 3 次，每次换策略，3 次失败升格给用户

---

## 四、TDD 实施规范

### 红-绿-重构循环

- **红**：先写测试，确认测试失败
- **绿**：写最少代码使测试通过
- **重构**：Review 通过后优化代码，确认测试仍通过

### 覆盖要求

- 每新增 API 端点 → contract test
- 每新增页面/组件 → component test
- 临界状态（401/403/404/500）必须有覆盖
- 例外：纯 UI 样式、文档、配置变更无需测试；原型代码可跳过但正式开发前必须补

### 测试 Harness

Harness 的目标是消灭手动验证——写完测试或代码后自动运行并返回结果。

核心命令（按项目适配）：
- 运行全部测试
- 运行单个测试文件/用例
- 监听模式（文件变更自动重跑）

每次写测试后立即运行确认红，每次写实现后立即运行确认绿。

---

## 五、模型策略：Fast 编码 + Pro 审查

- 代码编写使用快速模型（低成本）
- 每次代码改动后，自动用强模型审查（异步后台执行）
- 审查发现漏洞 → 强模型直接修复，无需再次审查
- 例外跳过 review：纯 UI/样式、注释/文档、配置持久化写入

---

## 六、需求澄清（Brainstorming）

任何新功能启动前，必须先通过苏格拉底式提问澄清需求：
- 用户到底想解决什么问题？（不是"想要什么功能"）
- 有没有更简单的方案？
- 边界条件是什么？什么情况下不该触发？
- 成功标准是什么？怎么验证？

结论写入 `spec.md` 的「需求背景」章节。禁止直接跳到方案设计。

---

## 七、原子任务分解

每个任务必须：
- 2-5 分钟可完成（超时则拆分，不足则合并）
- 独立可验证（不依赖其他未完成任务的上下文）
- 含精确文件路径（不模糊说"修改相关文件"）
- 含预期测试命令和期望结果
- 写清楚到「一个没有上下文的新人也能执行」

### 任务格式

```
- [ ] T### [P] [US#] 描述
  - 文件：[精确路径]
  - 验证：[测试命令或检查步骤]
  - 耗时：[分钟，2-5]
  - 依赖：[T### 或无]
```

| 要素 | 说明 |
|------|------|
| `T###` | 任务 ID（按执行顺序） |
| `[P]` | 可并行（可选） |
| `[US#]` | 用户故事标签 |
| 依赖 | 前置任务 ID |

### 阶段结构
1. 设置（项目初始化）→ 2. 基础（数据库、中间件等）→ 3+. 用户故事（P1→P2→P3）→ 最终：完善

---

## 八、Git Worktree 隔离

每个用户故事在独立 worktree 中开发，分支命名 `us-[###]-[short-name]`。

- 禁止在主分支直接开发（紧急 hotfix 除外）
- 非 git 项目或单文件脚本可跳过，但需文件备份
- 收尾：全量测试 → lint → 合并/PR/保留/丢弃四选一 → 清理 worktree → 更新文档

---

## 九、双阶段 Review

每个任务完成后按顺序执行两道审查：

**第一道：Spec Compliance**（当前会话内）
- 代码行为是否和 `spec.md` 验收场景一致？
- 是否有遗漏的 Given-When-Then 场景？
- 不通过 → 打回重写

**第二道：Code Quality**（异步后台）
- 逻辑漏洞、边界情况、安全风险、性能、类型安全
- 通过标准：无 Critical 问题
- 有漏洞 → 直接修复，修复后无需再次审查

---

## 十、执行纪律

### 2-Action 写盘

每完成一个子任务后，立刻把关键发现写盘。禁止把 10 个搜索结果全记在脑子里最后统一写。

写入目标：研究发现 → `research.md`；错误信息 → `progress.md`；文件理解 → `notes.md`（可选）。

### 3-Strike 错误协议

同一操作最多 3 次，每次换策略：
1. 诊断 + 针对性修复
2. 换方法/工具/库
3. 质疑假设 → 搜索 → 考虑改计划

3 次全失败 → 告知用户试了什么 + 每次失败原因 + 请求指导。

### 会话进度日志

`specs/[feature]/progress.md` — 断点续传依据。

```markdown
# 会话进度 — [功能名称]

## [YYYY-MM-DD] 会话 N
### 完成：- [X] T001 ...
### 阻塞：- [ ] T003 ...
### 关键决策：选择 A 而非 B，因为...
### 错误：| 错误 | Strike | 方案 |
### 下次：- [ ] T003 继续
```

更新时机：每完成任务 → 每遇错误 → 会话结束前。上下文刷新后先读 progress.md 再继续。

---

## 十一、用户故事与规范格式

### 用户故事

```
### 用户故事 N - [标题]（优先级：Pn）

[通俗语言描述]

**为什么这个优先级**：[原因]
**独立测试**：[如何独立测试]
**验收场景**：
1. **给定** [状态]，**当** [动作]，**那么** [结果]
```

### 功能规范要求

以「需求背景」开头，专注用户价值，使用清晰语言，定义可测量成功标准，考虑边界情况。禁止涉及实现细节和模糊语言。

### 技术计划结构

```
语言/版本 / 主要依赖 / 存储 / 测试 / 目标平台 / 项目类型 / 性能目标 / 约束 / 规模
```

默认项目结构：`src/（models/services/lib）` + `tests/（contract/integration/unit）`

---

## 十二、目录结构

```
├── AGENTS.md              # 本文件
├── specs/                 # 规范文档
│   └── [###-feature]/
│       ├── spec.md        # 功能规范（含需求背景）
│       ├── plan.md        # 技术计划
│       ├── research.md    # 技术研究（含探索过程）
│       ├── data-model.md  # 数据模型
│       ├── tasks.md       # 原子任务列表
│       ├── progress.md    # 会话进度日志
│       ├── contracts/     # 接口契约
│       └── checklists/    # 检查清单
```
